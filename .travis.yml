sudo: false
language: python
cache: pip
python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - 3.6
os:
  - linux
jobs:
  include:
    - script: tox
    - stage: release-test
      script: PYVER= ./release-test.sh
install:
  - pip install tox-travis
notifications:
  email: false
