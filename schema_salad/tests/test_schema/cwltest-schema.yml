$base: "https://w3id.org/cwl/cwltest#"
$graph:
  - $import: "../../metaschema/metaschema_base.yml"
  - name: TestCase
    type: record
    documentRoot: true
    fields:
      id:
        type: [int, string]
        jsonldPredicate:
          _type: "@id"
          identity: true
      label: string?
      doc:
        type: string?
        jsonldPredicate: "rdfs:comment"
      tags: string[]?
      tool:
        type: string
        jsonldPredicate:
          _type: "@id"
      job:
        type: string?
        jsonldPredicate:
          _type: "@id"
      should_fail: boolean?
      output:
        type: Any?
